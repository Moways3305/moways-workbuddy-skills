# {{DISPLAY_NAME}}｜产品设计

## 产品信息

- 产品 ID：`{{PRODUCT_ID}}`
- 工具形态：`{{ARTIFACT_TYPE}}`
- 产品负责人：{{OWNER}}
- 状态：设计中

## 用户任务

写清楚用户是谁、在什么场景下完成什么任务，以及当前方案的失败成本。

## 输入、输出与边界

| 项目 | 定义 |
|---|---|
| 输入 | 待补充 |
| 输出 | 待补充 |
| 可用数据 | 待补充 |
| 禁止数据 | 客户未脱敏资料、密钥和无授权的个人信息 |
| 外部动作 | 默认不安装、不发布、不发送 |

## 工具形态判断

按 `Prompt → Project Instructions → Skill → Plugin → MCP` 的最小充分原则选择，并记录排除其他形态的理由。

## 触发与不适用场景

- 正向触发：列出 5-15 个具体词或任务表达。
- 负向触发：列出至少 5 个相似但不应触发的请求。
- 不适用场景：列出权限、数据、能力和责任边界。

## 验收标准

至少覆盖触发率、误触发率、关键字段完整率、事实可追溯率、任务成功率、安全红线和宿主验证状态。
