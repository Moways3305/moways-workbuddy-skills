RELEASE_GATE: PASS
EVIDENCE_TYPE: security_review
PRODUCT_VERSION: 0.1.2
EVALUATED_AT: 2026-07-26T17:25:00+08:00

# v0.1.2 安全复核

## Luban TRACE 五维镜像评价

本表是 Luban 内部近似评价，不是 SkillHub 官方评分。

| 维度 | 内部近似分 / 5 | 证据等级 | 关键证据 | 主要缺口 |
|---|---:|---|---|---|
| T—可信任度 | 3.5 | static + dry_run | 无外部连接；不自动安装/发布；绝对本机路径、密钥特征、symlink、根外归档均阻断 | 未完成专业安全扫描 |
| R—可靠性 | 4.0 | dry_run | 21 项测试；原子初始化；JSON 回读；候选包完整性；结构化证据门禁 | 未在多宿主重复运行 |
| A—适用性 | 2.5 | static | 四个 Skill 有全流程/单阶段让位边界；每个 5/5/3 触发目录 | 未运行真实宿主触发率 |
| C—规范性 | 4.5 | static | 官方脚手架；官方 validator；SKILL.md 均低于 500 行；references/scripts/assets 分层 | 未做公开仓库安装说明和公网分发验证 |
| E—有效性 | 3.0 | dry_run | 一次正向 forward test 完成 S0-S6 并通过工作区校验 | 无 no-skill 配对增益和多人试点 |

内部近似总分：3.5 / 5。证据以最低覆盖项为准。

## T 维硬门

静态与 dry-run 范围内未发现以下 P0：

- 未披露的数据外传或凭据读取；
- 未授权删除、覆盖、发送、购买、部署或安装；
- 候选包纳入根目录外文件；
- 将静态证据冒充宿主 live 证据。

结论：内部候选的静态安全门通过；未完成专业安全检测，不得写“专业安全已通过”，不得用于公开发布。

## 发布边界

- 内部候选：允许在受控环境进入首次安装和真实触发试点。
- 公开发布：阻断，直到 `live_passed + verified + professional_passed`。
- 当前宿主状态：`runtime pending`。

## v0.1.1 回归边界

v0.1.1 新增报告类型、版本和带时区评估时间的机器校验，并以负向测试验证失配阻断；未增加外部连接、凭据访问、安装或发布权限。本轮没有执行新的专业安全审查，因此仍只保持 `static_passed`。

## v0.1.2 回归边界

v0.1.2 新增的示范件只含虚构数据、Markdown、JSON 和 SVG；验证器扫描绝对用户路径、凭据、邮箱、常见中国手机号/身份证/座机、内部域名、UNC 路径和私网 IPv4，并以固定角色集合核验示范发言者。本轮不发起外部连接，仍未执行专业安全审查，安全状态保持 `static_passed`。
